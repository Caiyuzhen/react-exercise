import { createContext } from "react"

const context = createContext()


export default context


// 然后再在需要用到的地方进行👇
// import MyContext from './src/38.jsx.单独定义context方法.js'